# Agent instructions — Crystalward

## Git

- Default branch is **`master`** (not `main`).
- After finishing meaningful work (docs, code, config), **commit and push to `origin/master` without asking**.
- Do not force-push or rewrite published history unless explicitly requested.
- Use clear commit messages (complete sentences, why + what).
